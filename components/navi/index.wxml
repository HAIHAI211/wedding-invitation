<view class="container">
    <image class="icon" src="{{ first ? disLeftSrc : leftSrc }}" bind:tap="onLeft"/>
    <text class="title">{{ title }}</text>
    <image class="icon" src="{{ last ? disRightSrc: rightSrc }}" bind:tap="onRight"/>
</view>
